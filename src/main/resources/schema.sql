CREATE TABLE ARTICLE (
                         ID BIGINT AUTO_INCREMENT PRIMARY KEY,
                         TITLE VARCHAR(255),
                         CONTENT TEXT,
                         CREATED_AT TIMESTAMP,
                         UPDATED_AT TIMESTAMP
);
